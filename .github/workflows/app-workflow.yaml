name: BMI application workflow
on:
  pull_request:
    branches: [ master ]
    paths:
      - 'src/**'
      - '.github/workflows/app-workflow.yaml'
  push:
    branches: [ master ]
    paths:
      - 'src/**'
      - '.github/workflows/app-workflow.yaml'